extends ../layout

block content
  form(action=`/users/edit/${user._id}`, method="POST")
    <div class="mb-3">
      <label for="username" class="form-label">Username</label>
      input#username.form-control(type="text", name="username", autocomplete="off", value=user.username)
    </div>
    <div class="mb-3">
      <label for="email" class="form-label">Email address</label>
      input#email.form-control(type="email", name="email", autocomplete="email", value=user.email)
    </div>
    <div class="mb-3">
      <label for="role" class="form-label">Role</label>
      select#role.form-select(name="role", value=user.role)
        option(value="admin", selected=user.role === "admin") Admin
        option(value="user", selected=user.role === "user") User
    </div>
    <button type="submit" class="btn btn-primary">Save</button>